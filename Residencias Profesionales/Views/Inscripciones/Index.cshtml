@model IEnumerable<Residencias.Models.Api.Inscripciones>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.grupo.nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UserId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.grupo.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UserId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.nu_inscripcion }) |
            @Html.ActionLink("Details", "Details", new { id=item.nu_inscripcion }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.nu_inscripcion })
        </td>
    </tr>
}

</table>
